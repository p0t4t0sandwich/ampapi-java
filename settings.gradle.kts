rootProject.name = "ampapi"

pluginManagement {
    repositories {
        mavenLocal()
        maven("https://maven.neuralnexus.dev/mirror")
        mavenCentral()
        gradlePluginPortal()
    }
}
